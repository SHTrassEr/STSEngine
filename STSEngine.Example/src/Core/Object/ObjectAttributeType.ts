namespace STSEngine.Example {

    export enum ObjectAttributeType {
        PlayerId = 50,
        Position,
        PositionPrecise,
        MinSpeed,
        MaxSpeed,
        Size,
        MoveDirection
    }
}